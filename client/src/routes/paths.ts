export const ROUTES = {
    home: "/",
    products: "/products",
    productDetails: "/products/:id",
    cart: "/cart",
    checkout: "/checkout",
    orders: "/orders",
    orderDetails: "/orders/:id",
    login: "/login",
    register: "/register",
} as const;
