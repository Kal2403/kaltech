export const ROUTES = {
    home: "/",
    products: "/products",
    productDetails: "/products/:id",
    cart: "/cart",
    checkout: "/checkout",
    login: "/login",
    register: "/register",
} as const;
