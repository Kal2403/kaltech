import type { OrderItem as OrderItemType } from '../../types/order.types';
export const OrderItem = ({ item }: { item: OrderItemType }) => <article className="flex gap-4 rounded-2xl border border-slate-200 bg-white p-4"><div className="h-24 w-24 shrink-0 overflow-hidden rounded-xl bg-slate-100"><img src={item.image || 'https://placehold.co/200x200?text=KalTech'} alt={item.name} className="h-full w-full object-contain p-2" onError={(event) => { event.currentTarget.src = 'https://placehold.co/200x200?text=KalTech'; }} /></div><div className="flex min-w-0 flex-1 flex-col justify-between sm:flex-row sm:items-center"><div><h3 className="font-black text-slate-950 sm:text-lg">{item.name}</h3><p className="mt-1 text-sm text-slate-500">Cantidad: {item.quantity}</p><p className="text-sm text-slate-500">Precio unitario: ${item.price.toFixed(2)}</p></div><p className="mt-3 text-lg font-black text-slate-950 sm:mt-0">${(item.price * item.quantity).toFixed(2)}</p></div></article>;
